version: 2

models:
  - name: raw_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
              
  - name: raw_orders
    columns:
      - name: orderid
        tests:
          - unique
          - not_null
              